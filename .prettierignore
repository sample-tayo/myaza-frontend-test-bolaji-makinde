# Keep build files in git but don't format them
dist
build

# Ignore generated files that are in git
public/sw.js
public/workbox-*.js
public/worker-*.js

# Any other files you want in git but don't want formatted